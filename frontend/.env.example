# Copy to .env.local to override the API address; Vite does not load .env.example.
# Include /api. Restart the dev server or rebuild after changing this value.
VITE_API_URL=https://gyproject.onrender.com/api
